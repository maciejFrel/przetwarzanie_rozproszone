
typedef struct ack
{
    int p; // clock
    int m; // liczba cuchów otaku
    int x; // skumulowana liczba cuchów dla przedstawiciela
};

typedef struct req
{
    // zadne dane nie są potrzebne
};

typedef struct release
{
    // zadne dane nie są potrzebne
};